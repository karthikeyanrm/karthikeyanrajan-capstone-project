# To install NPM on ubuntu

sudo apt update

# will install nodejs

sudo apt install nodejs

#it will install npm

sudo apt install npm

#checking the installed version of Node.js and npm

node --version

npm --version

#look for the package.json file in the project and installs the necessary dependencies

npm install

# Once the dependencies are installed, you can start the application

npm start
